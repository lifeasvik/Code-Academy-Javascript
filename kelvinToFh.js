Kelvin to Fahrenheit Converter

// Created a constant variable kelvin to hold current weather
const kelvin = 0;
// Created a variable that converts kelvin to celsius
let celsius = kelvin - 273;
// Created a variable to convert celsius to fahrenheit
let fahrenheit = celsius * (9/5) + 32;
// Convert fahrenheit from decimal to integer
fahrenheight = Math.floor(fahrenheit);

console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);